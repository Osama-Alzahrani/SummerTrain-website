<h1>Admin#index</h1>
<p>Find me in app/views/admin/index.html.erb</p>
<div class="d-flex justify-content-center">
  <table class="table table-dark table-striped mx-auto table-responsive text-center">
    <thead>
      <tr>
        <th scope="col">Id</th>
        <th scope="col">Post</th>
        <th scope="col">Approved?</th>
      </tr>
    </thead>
    <tbody class="text-center">
      <% @posts.each do |post| %>
        <tr>
          <th scope="row"><%= post.id %></th>
          <td><%= link_to post.title.truncate(15), admin_post_path(post) %></td>
          <td><%= post.approved? ? :✓ : "" %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>
