<div class="container text-center">
  <p>
    <%= link_to "Admin", admin_path %><%= " > "+t("admin.#{params[:action].downcase}") unless params[:action].eql?("index") %>
  </p>
  <div class="btn-group" style="direction: ltr;">
    <%= render "admin/link", resource: Post %>
    <%= render "admin/link", resource: Comment %>
    <%= render "admin/link", resource: User %>
  </div>
</div>
