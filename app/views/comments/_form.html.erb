<%= form_with(model: [post, post.comments.build]) do |form|%>
  <div class="form-control">
    <%= form.text_area :content%>
    <%= form.submit "Replay", class: "btn btn-primary mt-1" %>
  </div>
<%end%>
